value = 2
threshold = 5

if (value < threshold)
    // Increase value but cap it if it overshoots the threshold.
    value = value + 2
    if (value > threshold)
         value = threshold
    end
else
    // Reduce value if already at or above threshold.
    value = value - 1
end